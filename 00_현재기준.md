# FlowPulse 현재 기준

최종 갱신: 2026-08-11 (Asia/Seoul)

## 1. 현재 결론

- 운영 URL: `https://flowpulse.ai.kr/`
- 외부 HTTP 응답: `200`
- API 상태: `https://flowpulse.ai.kr/api/health` 정상
- Docker Compose의 `web`, `api`, `collector`, `postgres` 서비스가 실행 중이다.
- PostgreSQL healthcheck는 `healthy`다.
- KIS 수집 상태는 배포 직후 KOSPI·NASDAQ `healthy`를 확인했다.
- 서버의 KIS 키와 시크릿은 `/opt/flowpulse/.env`에 저장되어 있으며 권한은 `600`이다.
- 비밀값은 문서, 로그, 채팅에 기록하지 않는다.

## 2. 제품 구조

- 1번째 탭: 시장 Flow
- 2번째 탭: 관찰 후보 및 종목 분석
- 후보 파이프라인: 거래대금 상위 100 → 가격 조건 25 → 수급·전략 계산 10
- 사용자가 `관찰 시작`한 종목만 실시간 추적한다.
- 3번째 탭: 관심 종목에서 관찰·진입·청산을 관리한다.
- `일반 관심 등록`과 `관찰 시작`은 별도 상태다.
- 4번째 Feed와 5번째 Me는 현재 대기 화면이다.

기준 문서:

- `00_현재기준.md`
- `docs/종목분석_UI수정명세.md`
- `docs/관심종목_UI명세.md`

## 3. Oracle 서버

### 인스턴스

- 리전: Japan East (Tokyo), `ap-tokyo-1`
- 이름: `flowpulse`
- 상태: Running
- OS: Ubuntu 24.04
- Shape: `VM.Standard.E2.1.Micro` (1 OCPU, 1GB RAM)
- 공인 IP: `138.2.10.193`
- 사설 IP: `10.0.0.184`
- 사용자: `ubuntu`
- Instance OCID: `ocid1.instance.oc1.ap-tokyo-1.anxhiljrwgrb3jacilh7w4jpvoxdzej6wdiz7vhz5g7h2yxzzansajw37xsq`

### 네트워크

- VCN: `flowpulse-vcn`
- 공개 서브넷: `flowpulse-public`
- 인터넷 게이트웨이와 `0.0.0.0/0` 기본 경로 구성
- NSG: `ig-quick-action-NSG`
- NSG OCID: `ocid1.networksecuritygroup.oc1.ap-tokyo-1.aaaaaaaavx6q3ftcwftpwgyxf4ucxa767g7cn5yah7f27q3vddi6refe5lmq`
- 인바운드 TCP 22: `0.0.0.0/0`
- 인바운드 TCP 80: `0.0.0.0/0`, 설명 `FlowPulse HTTP`
- 아웃바운드: 전체 허용

### SSH 특이사항

- 로컬 실행 환경에서 공인 IP의 TCP 22는 timeout이 났지만 Oracle Cloud Shell에서는 즉시 연결됐다.
- 개인키: `/private/tmp/flowpulse-oracle` (ED25519, 로컬에만 존재)
- Cloud Shell의 구형 SSH는 `PubkeyAcceptedAlgorithms` 옵션을 모른다.
- Cloud Shell 접속 시 아래 구형 옵션명을 사용해야 한다.

```bash
ssh -o PubkeyAcceptedKeyTypes=+ssh-ed25519 \
  -o StrictHostKeyChecking=yes \
  -i flowpulse-oracle \
  ubuntu@138.2.10.193
```

- `sign_and_send_pubkey: no mutual signature supported`가 나오면 위 옵션이 빠진 것이다.
- 배포 완료 후 Cloud Shell에 올렸던 개인키와 배포 압축 파일은 삭제했다.

## 4. 서버 배포 상태

### 설치 경로

- 애플리케이션: `/opt/flowpulse`
- 환경 파일: `/opt/flowpulse/.env` (`chmod 600`)
- 임시 배포 파일과 로그는 배포 완료 후 삭제했다.

### 설치된 런타임

- Docker: `29.1.3`
- Docker Compose: `2.40.3`
- 2GB swap은 `deploy/oracle-install.sh`가 생성한다.

### Compose 서비스

- `flowpulse-web-1`: nginx, 호스트 `80:80`
- `flowpulse-api-1`: Node API, 컨테이너 내부 `0.0.0.0:8789`
- `flowpulse-collector-1`: KIS 데이터 수집기
- `flowpulse-postgres-1`: PostgreSQL 17, named volume `flowpulse_postgres_data`

PostgreSQL 데이터 볼륨은 재배포 시 삭제하지 않는다.

### 운영 확인 명령

```bash
cd /opt/flowpulse
sudo docker compose ps
sudo docker compose logs --tail=100 api collector
curl -fsS http://127.0.0.1/api/health
```

외부 확인:

```bash
curl -I https://flowpulse.ai.kr/
curl -fsS https://flowpulse.ai.kr/api/health
```

### 재배포

이 VM은 메모리가 작아 `api`와 `collector`의 동일 backend target을 병렬 빌드하면 이미지 export 충돌이 날 수 있다. 아래처럼 순차 빌드한다.

```bash
cd /opt/flowpulse
sudo docker compose build api
sudo docker tag flowpulse-api flowpulse-collector
sudo docker compose build web
sudo docker compose up -d --no-build
sudo docker compose ps
```

`failed to solve: image "docker.io/library/flowpulse-api:latest": already exists`가 나오면 실패한 이미지 산출물만 지우고 순차 빌드를 다시 한다. 데이터 볼륨은 지우지 않는다.

```bash
sudo docker image rm -f flowpulse-api flowpulse-collector
```

## 5. 배포 파일

- `Dockerfile`
- `compose.yml`
- `deploy/nginx.conf`
- `deploy/oracle-install.sh`
- `.env.deploy.example`

중요 구성:

- nginx `/api/` → `http://api:8789`
- API는 컨테이너 간 통신을 위해 `0.0.0.0:8789`에 바인딩한다.
- 웹은 호스트 TCP 80으로 공개한다.
- 로컬 개발 설정의 5174는 변경하지 않는다.
- 로컬 미리보기는 4173을 사용한다.

## 6. 이번 코드 변경

### 후보 파이프라인

- `api/kis.mjs`
- KOSPI·KOSDAQ 거래대금 결과를 합쳐 정렬한 뒤 최대 100개로 제한한다.
- 가격 조건 통과 종목은 25개로 제한한다.
- 상세 수급·전략 계산은 앞의 10개만 수행한다.

### 컨테이너 API 연결

- `api/server.mjs`
- API listen 주소를 `127.0.0.1`에서 `0.0.0.0`으로 변경했다.
- 이 변경이 없으면 별도 nginx 컨테이너가 API에 접근할 수 없다.

### 종목 검색 복구

- `src/StockAnalysis.tsx`
- 실시간 후보가 있으면 검색 대상이 후보 10개로 제한되던 회귀를 수정했다.
- 실시간 후보와 기존 검색 종목을 종목 코드 기준으로 중복 없이 병합한다.
- 4173 화면에서 `삼성전자` 검색 결과 노출과 선택을 직접 확인했다.
- 현재 검색은 전체 상장 종목 마스터 검색이 아니라 기존 검색 목록을 복구한 상태다. 전체 종목 검색이 필요하면 별도 종목 마스터 연동이 필요하다.

## 7. 검증 결과

- `npm test`: 14개 통과
- `npm run build`: 통과
- Vite production bundle 생성 확인
- 외부 앱 URL: HTTP 200
- 외부 `/api/health`: PostgreSQL 및 KIS ingestion 정상 응답

## 8. 현재 제한과 다음 작업

- 관심·관찰·진입·청산 상태는 현재 React 메모리에 있어 새로고침하면 사라진다.
- 다음 구현 우선순위는 사용자 관찰 상태를 PostgreSQL에 저장하는 API다.
- 전체 상장 종목 검색을 지원하려면 종목 마스터 데이터와 종목 상세 조회 API를 추가한다.
- HTTPS와 도메인은 아직 구성하지 않았다. 현재 운영 주소는 HTTP IP 접속이다.
- 배포 수정 전에는 반드시 로컬에서 `npm test`와 `npm run build`를 실행한다.
- `.env`와 KIS 비밀값을 출력하거나 저장소에 추가하지 않는다.

